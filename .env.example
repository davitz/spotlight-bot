# The Discord bot token — get one from https://discord.com/developers/applications
DISCORD_TOKEN=[discord token here]

# Default time (in seconds) to wait after the most recent new reaction before picking a winner.
# Can also be changed per channel with /set_interval or overridden when starting a loop.
REACTION_WAIT_SECONDS=30
